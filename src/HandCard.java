import java.util.ArrayList;
import java.util.List;

public class HandCard {
    List<Card> cards;

    public HandCard() {
        cards = new ArrayList<>();
    }

    public getValue() {
        for (Card card : cards) {

        }
    }

    public List<Card> getCards() {
        return cards;
    }
}
